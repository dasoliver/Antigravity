{
  "name": "antigravity",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @antigravity/web dev",
    "build": "pnpm --filter @antigravity/web build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "~5.4.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
